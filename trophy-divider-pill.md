---
"@saasmakers/ui": patch
---

Add a `pill` prop to `BaseDivider` to render the centered title as a dark uppercase pill with full-width side lines. Remove the `margin` prop in favor of a fixed `my-6` default and consumer `class` overrides. Add a `hero` slot to `LayoutBottomSheet` for content above the scroll area. Remove the default browser focus outline on dialog panels.
